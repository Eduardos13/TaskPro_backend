description: Conflict error
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - error
      properties:
        status:
          type: integer
          example: 409
        message:
          type: string
          example: ConflictError
        error:
          type: string
          example: This email is already in use
