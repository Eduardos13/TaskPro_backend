description: Conflict error
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - error
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: ConflictError
        error:
          type: string
          example: Invalid password or email
